<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Loaner Dashboard</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>

<body>
  <nav>
    <div class="search-bar">

      <div class="profile-btn" onclick="toggleSidebar()">
        <img
          src="https://images.squarespace-cdn.com/content/v1/5b328976aa49a1b7b46b89c3/1530039440176-O24B7OCR4WPIAI62JZTW/CompSci.High_Circle_Logo.png?format=500w"
          alt="Profile" height="50px" />
      </div>



    </div>
  </nav>

  <div class="sidebar" id="sidebar">
    <h3>compscihigh/h3>
      <p>447 Wales Avenue
        Bronx, New York 10455

        (347) 352-1528

        info@compscihigh.org</p>
      <!-- add more account options here -->
  </div>

  <div class="actions">
    <a href="/form.html" class="add-btn">Add Loaner</a>
  </div>

  <div class="container" id="loanerCards">

    <% for (let i=0; i < loaner.length; i++) { %>

      <div class="card">
        <p><strong>Loaner Name:</strong>
          <%= loaner[i].loanername %>
        </p>
        <p><strong>Status:</strong>
          <%= loaner[i].working %>
        </p>
        <p><strong>Student:</strong>
          <%= loaner[i].student %>
        </p>
        <p><strong>Date Taken:</strong>
          <%= loaner[i].datetaken %>
        </p>
        <form onsubmit="editstatus(event, '<%= loaner[i]._id %>')">
          <input type="text" name="working" value="" />
          <div class="button-container">
            <button type="submit" class="update-btn">Change Status</button>
            <button type="button" class="update-btn" onclick="deletebutn('<%= loaner[i]._id %>')">Return</button>
          </div>
        </form>
        <!-- Link this to app.patch route -->
      </div>

      <% } %>

  </div>

  </div>

  <script>
    function toggleSidebar() {
      document.getElementById('sidebar').classList.toggle('active');
    }



    // function showDeletePopup() {
    //   document.getElementById('deletePopup').style.display = 'block';
    // }

    // function confirmDelete(confirm) {
    //   if (confirm) {
    //     // Use fetch or axios to call app.delete route with selected IDs
    //     alert("Deleting selected loaners...");
    //   }
    //   document.getElementById('deletePopup').style.display = 'none';
    // }

    // const searchInput = document.getElementById('search');
    // const suggestionsBox = document.getElementById('suggestions');

    // const loaners = ['LP001', 'LP002', 'HP123', 'CHROME001']; // Replace with data from DB

    // searchInput.addEventListener('input', function () {
    //   const value = this.value.toLowerCase();
    //   suggestionsBox.innerHTML = '';
    //   if (value) {
    //     const matches = loaners.filter(l => l.toLowerCase().includes(value));
    //     matches.forEach(match => {
    //       const div = document.createElement('div');
    //       div.className = 'suggestion';
    //       div.textContent = match;
    //       div.onclick = () => {
    //         searchInput.value = match;
    //         suggestionsBox.style.display = 'none';
    //       }
    //       suggestionsBox.appendChild(div);
    //     });
    //     suggestionsBox.style.display = 'block';
    //   } else {
    //     suggestionsBox.style.display = 'none';
    //   }
    // });


  </script>

  <script src="/script.js"></script>

</body>

</html>